# Integration tests

Integration tests for the browser adapters land in M2 (`OAF-BROWSER-*`) and
run headless Chromium against the local fixture server (`OAF-TEST-001`). Until
then this directory is intentionally empty; `pnpm test:integration` passes with
no tests.
