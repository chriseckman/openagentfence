---
"@openagentfence/policy": minor
---

Add the bounded, versioned policy document boundary: a published JSON Schema,
branded immutable `ValidatedPolicyDocument`, and strict YAML/JSON loading that
rejects provider configuration and unsafe parser features. Engine construction
and public `loadPolicy()` remain deferred to PS-002.
