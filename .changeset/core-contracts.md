---
"@openagentfence/core": minor
---

Implement the M1 core security contracts and orchestrator: task-contract
validation, secure-default capability envelope, policy engine interface and
secure-default engine, fixed-precedence risk aggregator, scanner API with
least-privilege plugin views, phase orchestrator with deadlines and fail-closed
timeouts, redacted trace writer, secret handle codec, `SecuritySession` /
`OpenAgentFence` facade with deny-by-default approval and the recorded escape
hatch, and the framework-neutral `BrowserAdapter`, `GuardModelProvider`, and
in-page probe contracts.
