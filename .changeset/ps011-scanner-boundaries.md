---
"@openagentfence/scanners": minor
---

Close PS-011 perception-scanner bounds: encoded normalization now reports
input/output/depth/deadline refusal explicitly, injection rule packs are
validated inert data rather than executable regular-expression objects, and
the visibility classifier records expanded deterministic signal evidence and
probe-truncation low-confidence results.
