# Screenshot-first composition

Screenshot-first context includes the adapter screenshot and only
firewall-scanned, provenance-bearing visible text. Hidden DOM text is withheld.
v0.1 does not compare pixels with DOM/ARIA or claim visual-injection detection;
all proposed actions still pass the deterministic Action Guard.

See [`index.ts`](index.ts) for the compile-checked helper.
